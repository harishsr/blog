

- @articles.each do |article|
  %h3= link_to article.title, article
  .time= article.created_at.strftime("Created on %m.%d.%Y")
  .time
    By 
    = article.user.email
  %p= simple_format(article.entry)
  %hr

= will_paginate @articles