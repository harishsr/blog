.row
  = render 'shared/errors'

%h2 Make an Update:
= form_for @article, method: :patch, html: { multipart: true }  do |f|
  %p
    = f.label :title
    %br
    = f.text_field :title

  %p
    = f.label :entry, "Create your entry"
    %br
    = f.text_area :entry, rows: 20

  %p
    = f.submit
  %p.picture
    = f.file_field :picture, accept: 'image/jpeg,image/gif,image/png'

%script{type: "text/javascript", src: "images.js"}